# look at local.mailserver-config for a documented full-fledged example

smtp-incoming:
    hostname: mail.maurus.net


smtp-outgoing:
    hostname: smtp.maurus.net


imap-incoming:
    hostname: mail.maurus.net

# vim: syntax=yaml

