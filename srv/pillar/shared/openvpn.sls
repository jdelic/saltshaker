# OpenVPN server configuration

# openvpn:
#     bind-ip: a.b.c.d (usually just leave this empty, it will use the node's external interface IP)
#     sslcert: default (use the environment's wildcard)
#     sslkey: default (use the environment's wildcard)
