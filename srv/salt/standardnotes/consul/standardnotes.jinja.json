{
    "service": {
        "name": "{{service}}-{{suffix}}",
        "tags": ["smartstack:external", "smartstack:port:{{port}}", "smartstack:mode:http",
                 "smartstack:hostname:{{hostname}}", "smartstack:protocol:https", "smartstack:https-redirect"],
        "address": "{{ip}}",
        "port": {{port}}
        {# TODO: add service checks here? #}
    }
}
