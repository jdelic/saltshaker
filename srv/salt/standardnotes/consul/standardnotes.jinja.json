{
    "service": {
        "name": "{{service}}-{{suffix}}",
        "tags": [
            "smartstack:external",
            "smartstack:port:{{port}}",
            "smartstack:https-redirect",
            "smartstack:routing:hostname",
            "smartstack:hostname:{{hostname}}",
            "smartstack:mode:http",
            "smartstack:protocol:https"
        ],
        "address": "{{ip}}",
        "port": {{port}}
        {# TODO: add service checks here? #}
    }
}
