{
    "acl": {
        {% if not agent_acl_token.firstrun %}
            "tokens": {
                "agent": "{{agent_acl_token}}",
                "default": "{{agent_acl_token}}"
            }
        {% endif %}
    }
}
