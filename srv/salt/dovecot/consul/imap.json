{
    "services": [
         {
            "name": "mail-imap",
            "tags": [],
            "address": "{{pillar['imap'].get('ip', grains['ip_interfaces'][pillar['ifassign']['external']][pillar['ifassign'].get('external-ip-index', 0)|int()])}}",
            "port": 143
            {# TODO: add service checks here? Or add haproxy in front? #}
        },
        {
            "name": "mail-imap-ssl",
            "tags": [],
            "address": "{{pillar['imap'].get('ip', grains['ip_interfaces'][pillar['ifassign']['external']][pillar['ifassign'].get('external-ip-index', 0)|int()])}}",
            "port": 993
            {# TODO: add service checks here? Or add haproxy in front? #}
        }
    ]
}
